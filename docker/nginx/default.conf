# Static HHIO site + proxy API paths to FastAPI (shared DB health, future APIs).

upstream hhio_api {
    server api:8000;
}

server {
    listen 80 default_server;
    # default_server catches requests by raw IP; names cover local and production domain
    server_name localhost 127.0.0.1 www.heres-hopin-it-opens.com heres-hopin-it-opens.com;
    root /usr/share/nginx/html;
    index index.html;

    # gzip text assets
    gzip on;
    gzip_types text/css application/javascript application/json text/plain;

    location /health {
        proxy_pass http://hhio_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /docs {
        proxy_pass http://hhio_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /openapi.json {
        proxy_pass http://hhio_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
